import React from 'react'

const SelectRow = () => {
  return (
    <div>
        <label htmlFor={label}> {label} </label>
        <select  id={label}>
            {}
        </select>
    </div>
  )
}

export default SelectRow