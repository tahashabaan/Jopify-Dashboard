import { check } from "express-validator";
import validateMidelware from "../../middlewares/validateMidelware";

export const createJobValidate = [
    check().<i class="fas fa-node    "></i>

    validateMidelware
]